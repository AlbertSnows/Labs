import java.util.*; 


public class Lab5{
	public static void main(String[] args){
		Scanner keyboard = new Scanner(System.in); 
		String input; 
		String password; 
		String stringLetter; 

		
		int digit; 
		
		int indexDigit; 
		int searchDigit; 
		int cycleDigit = 0; 
		int indexExit = 0;
		
		char letter; 
		char[] specialChars = {'!', '@', '#', '$', '*'};

		
		boolean exitWhile = false;
		
while (exitWhile != true){

		System.out.println("Type in a password. It must be a minimum of 8 characters."); 
		System.out.println("At least one of the following characters must also be included: !@#$*"); 
		System.out.println("It must also contain at least one digit.");
		System.out.println("Also enter a '/' to exit the program. Do not use a '/' in your password.");
		password = keyboard.nextLine(); 
		
		indexExit = password.indexOf('/'); 
		
		if (indexExit > -1){
			exitWhile = true; 
			indexExit = 0;
			System.out.println("Goodbye!"); 
			System.exit(0);
		}
		
		for (searchDigit = 0; searchDigit < password.length(); searchDigit++)
			{
				letter = password.charAt(searchDigit); 
				
				if (Character.isLetter(letter)){
					System.out.println("This character is not a digit:" + letter);
				}
				else {
					stringLetter = Character.toString(letter); 
					digit = Integer.parseInt(stringLetter); 
					System.out.println("Digit: " + digit);
					
					//while (cycleDigit != 9){
					//
					//	cycleDigit++; 
					//}
				}
				
				
				
					
			}
		
		
		
		System.out.println("Password: " + password); 
		System.out.println("Password length: " + password.length());
		
		if (password.length() < 8){
			System.out.println("Make sure your password is at least 8 characters long!"); 
	
		} //password must be at least 8 characters

//if (){} //password must contain a !,@,#,$, or *

				
	System.out.println(); 
}

	keyboard.close(); 
	}//end main
}//end class
